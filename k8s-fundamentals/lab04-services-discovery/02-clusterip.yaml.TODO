# TODO — author the ClusterIP Service.
# Required:
#   apiVersion: v1
#   kind: Service
#   metadata.name: web
#   spec.type: ClusterIP
#   spec.selector: { app: web }
#   spec.ports[0]: name=http, port=80, targetPort=80
