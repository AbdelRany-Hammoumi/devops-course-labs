# TODO — ServiceAccount "deploy-bot" + Role "deployer" + RoleBinding linking them.
# Role allows full CRUD on Deployments/ReplicaSets (apps/v1) and Pods/ConfigMaps/Services (core).
# Excludes Secrets (intentional — see lab Step 5 verification).
