apiVersion: v1
kind: Pod
metadata:
  name: broken-config
spec:
  containers:
    - name: web
      image: nginx:1.27-alpine
      ports:
        - containerPort: 80
      resources:
        requests:
          cpu: "4"            # ← intentional bug: more than any kind Node has
          memory: 64Mi
