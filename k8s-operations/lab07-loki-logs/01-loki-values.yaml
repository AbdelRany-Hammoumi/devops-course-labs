loki:
  enabled: true
  isDefault: false
  persistence:
    enabled: false           # filesystem in-memory for the lab; production uses S3

promtail:
  enabled: true

grafana:
  enabled: false             # we use the Grafana from kube-prometheus-stack (lab 06)

prometheus:
  enabled: false             # already installed by kube-prometheus-stack
